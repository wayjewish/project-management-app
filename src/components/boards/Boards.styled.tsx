import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const BordersWrap = styled(Box)(() => ({}));

export const CircularProgressBox = styled(Box)(() => ({
  display: 'flex',
  justifyContent: 'center',
}));
