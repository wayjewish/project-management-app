import { styled } from '@mui/material/styles';
import { Typography, Container, Grid, Box } from '@mui/material';



export const TypographyBoardTitle = styled(Typography)(() => ({
padding: '30px 0'}));
